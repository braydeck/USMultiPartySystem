# Verified against the environment these analyses actually ran in (Python 3.13).
pandas==2.3.2
numpy==2.3.3
scipy==1.16.2
statsmodels==0.14.6
matplotlib==3.10.6
bambi==0.13.0
pymc==5.25.1
arviz==0.22.0

# Optional — only needed to (re)build raw data, not for the committed pipeline:
pyreadr>=0.5.0        # acquire.py: read V-Dem .RData -> vdem_slim.parquet (skipped if the parquet is present)
linearmodels>=6.0     # analyze.py only (legacy frequentist tier analysis; its outputs are in outputs/archive/)
