# Data files (too large for GitHub, download from Harvard Dataverse)
dataverse_files/*.csv
dataverse_files/*.dta

# Jupyter checkpoints
.ipynb_checkpoints/

# Python cache
__pycache__/
*.pyc

# OS files
.DS_Store

# Large CES raw survey files (too big for GitHub — keep locally only)
data/raw/2024 CES Base/CCES24_Common_OUTPUT_vv_topost_final.dta
data/raw/2024 CES Base/CCES24_Common_OUTPUT_vv_topost_final.csv
data/raw/2020 CES Base/CES20_Common_OUTPUT_vv.dta
data/raw/2020 CES Base/CES20_Common_OUTPUT_vv.csv

# Large CES raw survey files dropped in repo root (keep locally only)
/CCES24_Common_OUTPUT_vv_topost_final*.dta
/CCES24_Common_OUTPUT_vv_topost_final*.csv

# ANES data (removed — not used; redistribution-restricted)
data/raw/2024 anes data/
*anes_timeseries*

# Post recommendations / drafts (keep locally only)
results/post_recs/
.wrangler/
quiz_rescored.csv
quiz_backup_pre_rescore.csv
quiz_rescore_report.md
quiz_key_positions_review.md

# Personal drafts, outreach notes, and local-only working files
UNTRACKED/

# Cross-wave analysis: large regenerable fit intermediates (CSV/md outputs tracked)
analysis/efa/previous_years/outputs/*/fit_results.pkl
analysis/efa/previous_years/outputs/*/independent_fit.pkl

# Claude Code local tooling. settings.json is shared; skills are personal and
# worktrees/ holds whole repo checkouts, which must never be committed.
.claude/skills/
.claude/worktrees/

# Alaska Dominion CVR exports (~1.5 GB each; download URLs in pipeline/rcv/README.md)
data/raw/rcv/
